package ddl

// DDL package compiler provides methods to modify existing complied Hugr GraphQL schemas
// like adding new fields, creating data objects on the fly, etc.

// Create a new module (schema)

// Create a new table (data object)

// Create a new view (data object)

// Add a new field to an existing data object

// Rename an existing field in a object
// Create a new
