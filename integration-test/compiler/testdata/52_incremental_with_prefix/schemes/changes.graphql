"""Review added incrementally with prefix"""
type Review @table(name: "reviews") {
  id: Int! @pk
  hotel_id: Int! @field_references(
    references_name: "Hotel"
    field: "id"
    query: "hotel"
    references_query: "reviews"
  )
  rating: Float!
  comment: String
}
