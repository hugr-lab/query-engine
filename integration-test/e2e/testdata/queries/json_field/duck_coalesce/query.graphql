{
  local_db {
    json_field_demo(
      filter: {
        data: {
          field: { path: "user.age", coalesce: 99, int: { lt: 18 } }
        }
      }
      order_by: [{ field: "id", direction: ASC }]
    ) {
      id
    }
  }
}
