{
  local_db {
    json_field_demo(
      filter: { data: { field: { path: "account.balance", bigInt: { gte: 3000000000 } } } }
      order_by: [{ field: "id", direction: ASC }]
    ) {
      id
    }
  }
}
