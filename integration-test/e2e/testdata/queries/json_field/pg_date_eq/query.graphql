{
  pg_store {
    json_field_demo(
      filter: { data: { field: { path: "signup.day", date: { eq: "2024-01-15" } } } }
      order_by: [{ field: "id", direction: ASC }]
    ) {
      id
    }
  }
}
