{
  local_db {
    json_field_demo(
      filter: { data: { field: { path: "event.local_dt", dateTime: { eq: "2024-06-11T10:00:00" } } } }
      order_by: [{ field: "id", direction: ASC }]
    ) {
      id
    }
  }
}
