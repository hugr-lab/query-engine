{
  jq(query: ".pg_store.products | map({name, price})") {
    pg_store {
      products(order_by: [{field: "id", direction: ASC}], limit: 3) {
        id
        name
        price
      }
    }
  }
}
