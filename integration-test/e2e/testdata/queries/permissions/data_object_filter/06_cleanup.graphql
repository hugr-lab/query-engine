mutation {
  core {
    delete_role_permissions(filter: { role: { eq: "e2e_perm_scoped" } }) {
      success
    }
    delete_roles(filter: { name: { eq: "e2e_perm_scoped" } }) {
      success
    }
  }
}
