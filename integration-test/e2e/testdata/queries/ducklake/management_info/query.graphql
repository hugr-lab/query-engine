{
  function {
    core {
      ducklake_info(name: "dl_test") {
        name
        snapshot_count
        current_snapshot
        table_count
        schema_count
        view_count
        schema_version
        metadata_backend
        ducklake_version
      }
    }
  }
}
