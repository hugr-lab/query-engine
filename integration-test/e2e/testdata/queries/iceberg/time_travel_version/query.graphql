{
  ice_test {
    default {
      default_sensors(order_by: [{ field: "id", direction: ASC }]) @at(timestamp: "2099-01-01T00:00:00Z") {
        id
        name
        temperature
        humidity
      }
    }
  }
}
