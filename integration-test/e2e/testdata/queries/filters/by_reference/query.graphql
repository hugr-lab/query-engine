{
  pg_store {
    products(filter: { category: { id: { eq: 2 } } }, order_by: [{field: "id", direction: ASC}]) {
      id
      name
      price
      category {
        id
        name
      }
    }
  }
}
